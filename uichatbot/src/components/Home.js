const Home = () => {
    return (
        <div>
            <h1>Welcome to the Home Page</h1>
            <p>This is a protected route, accessible only when logged in.</p>
        </div>
    );
}

export default Home;